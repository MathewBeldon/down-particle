<div class="min-h-screen bg-gray-900 p-6">
  <div class="max-w-md mx-auto">
    <div class="mb-8">
      <h1 class="text-2xl font-bold text-white mb-2">New Task</h1>
      <p class="text-gray-400">Add a new task to "<%= @column.title %>"</p>
    </div>

    <div class="bg-gray-800 rounded-lg p-6">
      <%= render 'form' %>
    </div>
  </div>
</div>

<%= link_to "Back to Column", column_path(@column) %>